name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: Rustfmt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust + rustfmt
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust + clippy
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache build
        uses: Swatinem/rust-cache@v2

      - name: Run clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache build
        uses: Swatinem/rust-cache@v2

      - name: Install debugfs
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y e2fsprogs

      - name: Test core on Linux
        if: runner.os == 'Linux'
        working-directory: hashtime
        run: cargo build --release && sudo -E env "PATH=$PATH" cargo test --release

      - name: Test core on macOS
        if: runner.os == 'macOS'
        working-directory: hashtime
        run: cargo test --release

      - name: Test core on Windows
        if: runner.os == 'Windows'
        working-directory: hashtime
        run: cargo build --release && cargo test --release
        # shell: bash
        # run: |
        #   if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
        #     sudo -E env "PATH=$PATH" cargo test --release
        #   else
        #     cargo test --release
        #   fi

      - name: Test CLI
        working-directory: hashtime-cli
        run: cargo test --release

  doc:
    name: Docs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build docs
        run: cargo doc --no-deps --all
        env:
          RUSTDOCFLAGS: -D warnings

  test-python:
    name: Python Test (${{ matrix.os }}-py${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ['3.10', '3.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
          cache: 'pip'

      - name: Install maturin
        run: pip install maturin pytest

      - name: Build wheel
        working-directory: hashtime-py
        run: maturin build --release

      - name: Install wheel
        run: pip install --find-links target/wheels hashtime

      - name: Run Python tests
        working-directory: hashtime-py
        run: pytest tests/

  build-cli:
    name: Build CLI (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-14
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache build
        uses: Swatinem/rust-cache@v2

      - name: Add target
        run: rustup target add ${{ matrix.target }}

      - name: Build
        working-directory: hashtime-cli
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package binary
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            powershell -Command "Compress-Archive -Path hashtime.exe -DestinationPath hashtime-${{ matrix.target }}.zip"
          else
            tar -czf hashtime-${{ matrix.target }}.tar.gz hashtime
          fi

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/hashtime-${{ matrix.target }}.*

  build-wheel:
    name: Build Wheel (${{ matrix.os }}-py${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ['3.10', '3.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Cache build
        uses: Swatinem/rust-cache@v2

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          args: --release -i python${{ matrix.python }}
          working-directory: hashtime-py

      - name: Upload wheel
        uses: actions/upload-artifact@v7
        with:
          name: wheel-${{ matrix.os }}-py${{ matrix.python }}
          path: target/wheels/hashtime-*.whl
